export default {}

// Only needed if you need to use a theme that differs from Hero's default

// import { Theme, theme as heroTheme } from '@conexasaude/hero'
// import merge from 'merge-deep'

// const theme: Theme = merge(heroTheme, {
// ...your custom theme here
// })

// export default theme
